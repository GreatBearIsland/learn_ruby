def bubble_sort(array)
    
    sorted_array = []
    n = length.array
    #n.times do array.each_with_index
        array.take(2) = evaluate_array
         array.map  {
            if evaluate_array.at[0] > evaluate_array[1]
                    evaluate_array.reverse
                    sorted_array.push()
            else sorted_array.push()
            end}

            p array 
            p evaluate_array
            p sorted_array
end

array[1,2,3]