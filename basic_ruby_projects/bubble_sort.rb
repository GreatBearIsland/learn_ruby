def bubble_sort(array)
    
    sorted_array = []
    n = length.array
    #n.times do array.each_with_index
    array.take(2) = evaluate_array
    array.map  {|a| a < a+1 
        if a > b
        sorted_array.push()}
    